import 'package:get_it/get_it.dart';

GetIt locator = GetIt.instance;

void setupLocator() {
  // locator.registerLazySingleton(() => TodoApi());
  // locator.registerLazySingleton(() => AuthApi());
  // locator.registerLazySingleton(() => PasswordApi());
}